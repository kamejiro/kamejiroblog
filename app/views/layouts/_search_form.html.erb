<%=form_with(url: search_path, method: :get, local:true, id: "search_form") do |f|%>
  <%= f.text_field :keyword, value: @keyword, class: 'form-control' %>
  <%= f.submit "検索", class: "btn btn-primary" %>
<% end %>

